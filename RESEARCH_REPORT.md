오# 🔬 NICE POS 리서치 보고서

> **작성일**: 2026-02-11  
> **목적**: NICE POS POC 평가 + 실제 프로젝트 적용을 위한 심층 리서치  
> **대상**: 나이스페이먼츠(NicePay) 결제 API 연동

---

## 📋 목차

1. [POC 현황 평가](#1-poc-현황-평가)
2. [나이스페이 공식 API 리서치](#2-나이스페이-공식-api-리서치)
3. [POC vs 실제 API 차이점 분석](#3-poc-vs-실제-api-차이점-분석)
4. [프로젝트 적용을 위한 필수 작업](#4-프로젝트-적용을-위한-필수-작업)
5. [당신이 봐야 할 자료 (숙지 사항)](#5-당신이-봐야-할-자료-숙지-사항)
6. [실제 구현 로드맵](#6-실제-구현-로드맵)

---

## 1. POC 현황 평가

### ✅ 현재 POC에서 잘 된 점

| 항목 | 평가 | 설명 |
|------|------|------|
| **프로젝트 구조** | ⭐⭐⭐⭐⭐ | Next.js App Router 기반, API Routes 분리, 타입정의 분리 등 아키텍처가 실제 프로젝트에 적용가능한 수준 |
| **TypeScript 타입 정의** | ⭐⭐⭐⭐ | `types/payment.ts`에 결제 관련 타입이 체계적으로 정의됨 (enum, interface 등) |
| **API 엔드포인트 설계** | ⭐⭐⭐⭐ | RESTful한 API 설계 (POST /api/payment/card, DELETE /api/payment/card 등) |
| **입력 검증** | ⭐⭐⭐⭐ | API Routes에서 필수 필드 검증 로직이 구현되어 있음 |
| **시뮬레이션 모드** | ⭐⭐⭐⭐⭐ | 실제 NICE API 없이도 전체 흐름을 테스트할 수 있는 Mock 시스템 |
| **에러 핸들링** | ⭐⭐⭐ | try-catch 기본 구현, 에러 코드 체계 있음 |
| **UI 컴포넌트** | ⭐⭐⭐⭐ | 카드, QR, 현금영수증, 내역 조회 4개 핵심 컴포넌트 완성 |
| **결제 내역 관리** | ⭐⭐⭐⭐ | 필터링, 페이지네이션, 취소 기능 포함 |
| **유틸리티** | ⭐⭐⭐⭐ | 금액 포맷, 날짜 포맷, 카드 마스킹 등 실용적 유틸 |
| **README 문서** | ⭐⭐⭐⭐⭐ | API 문서, 아키텍처 다이어그램, 시퀀스 다이어그램까지 매우 완성도 높음 |

### ⚠️ POC로서의 부족한 점 (프로덕션 전 개선 필요)

| 항목 | 현재 상태 | 필요한 개선 |
|------|----------|------------|
| **실제 NICE API 연동** | ❌ 시뮬레이션만 있음 | 나이스페이 JS SDK + Server Auth 모델 구현 필요 |
| **결제창 (Payment Window)** | ❌ 없음 | 나이스페이 JS SDK의 `AUTHNICE.requestPay()` 연동 필요 |
| **인증/보안** | ❌ 없음 | Basic Auth (clientId:secretKey) 또는 Bearer Token 인증 구현 |
| **위변조 검증** | ❌ 없음 | `signature` 검증 로직 (SHA-256 해시) 구현 필요 |
| **망취소(Net Cancel)** | ❌ 없음 | 네트워크 장애 시 결제 취소 로직 필수 |
| **웹훅(Webhook)** | ❌ 없음 | 결제 상태 변경 알림 수신 엔드포인트 필요 |
| **데이터베이스** | ❌ In-memory Map | PostgreSQL/MySQL 등 영구 저장소 필요 |
| **테스트 코드** | ❌ 없음 | Unit/Integration 테스트 |

### 📊 POC 종합 평가

> **결론: POC로서는 매우 충분합니다!** ⭐⭐⭐⭐ (4/5)
>
> - 전체적인 결제 흐름(승인 → 조회 → 취소)이 잘 구현되어 있음
> - 아키텍처 설계가 확장 가능한 구조로 되어 있음
> - UI/UX가 실제 POS 시스템처럼 직관적
> - **다만, 실제 프로젝트에 적용하려면 "나이스페이 공식 API 연동"이 핵심 작업**

---

## 2. 나이스페이 공식 API 리서치

### 2.1 나이스페이 결제 흐름 (Server 승인 모델) ⭐ 가장 중요

나이스페이는 **Server 승인 모델**과 **Client 승인 모델** 2가지를 제공합니다.  
우리 프로젝트에는 **Server 승인 모델**이 적합합니다 (보안상 권장).

```
┌──────────┐     ┌──────────────┐     ┌─────────────────┐     ┌──────────┐
│  결제자   │     │  프론트엔드   │     │ 가맹점 서버      │     │ 나이스페이 │
│ (브라우저)│     │ (Next.js)    │     │ (API Routes)    │     │   API    │
└────┬─────┘     └──────┬───────┘     └────────┬────────┘     └────┬─────┘
     │                  │                      │                    │
     │ 1. 결제 요청      │                      │                    │
     │─────────────────►│                      │                    │
     │                  │                      │                    │
     │ 2. AUTHNICE.requestPay()                │                    │
     │  (JS SDK 결제창 호출)                    │                    │
     │◄─────────────────│                      │                    │
     │                  │                      │                    │
     │ 3. 카드사 인증 진행│                      │                    │
     │──────────────────┼──────────────────────┼───────────────────►│
     │                  │                      │                    │
     │ 4. 인증 결과 POST │ (returnUrl로 전달)   │                    │
     │                  │                      │◄───────────────────│
     │                  │                      │  tid, authResult   │
     │                  │                      │                    │
     │                  │  5. 위변조 검증       │                    │
     │                  │  (signature 체크)     │                    │
     │                  │                      │                    │
     │                  │  6. 승인 API 호출      │                    │
     │                  │                      │───────────────────►│
     │                  │                      │ POST /v1/payments/  │
     │                  │                      │    {tid}            │
     │                  │                      │                    │
     │                  │  7. 승인 결과         │                    │
     │                  │                      │◄───────────────────│
     │                  │                      │                    │
     │ 8. 결과 표시      │◄─────────────────────│                    │
     │◄─────────────────│                      │                    │
```

### 2.2 핵심 API 엔드포인트 목록

나이스페이 공식 REST API의 전체 엔드포인트 목록입니다.

| API | Method | Endpoint | 설명 |
|-----|--------|----------|------|
| **AccessToken 발급** | `POST` | `/v1/access-token` | Bearer Token 인증 시 사용 |
| **결제창 승인** | `POST` | `/v1/payments/{tid}` | 결제(승인) 요청 ⭐ |
| **취소 요청** | `POST` | `/v1/payments/{tid}/cancel` | 결제 취소/환불 ⭐ |
| **거래조회 (tid)** | `GET` | `/v1/payments/{tid}` | tid로 거래 조회 ⭐ |
| **거래조회 (orderId)** | `GET` | `/v1/payments/find/{orderId}` | 주문번호로 거래 조회 |
| **빌키 발급** | `POST` | `/v1/subscribe/regist` | 정기결제용 빌키 발급 |
| **빌키 승인** | `POST` | `/v1/subscribe/{bid}/payments` | 빌키 기반 결제 승인 |
| **빌키 삭제** | `POST` | `/v1/subscribe/{bid}/expire` | 빌키 만료 처리 |
| **약관 조회** | `GET` | `/v1/terms` | 약관 조회 |
| **현금영수증 발급** | `POST` | `/v1/receipt/` | 현금영수증 발급 ⭐ |
| **현금영수증 취소** | `POST` | `/v1/receipt/{tid}/cancel` | 현금영수증 취소 |
| **현금영수증 조회** | `GET` | `/v1/receipt/{tid}` | 현금영수증 조회 |
| **카드 이벤트 조회** | `GET` | `/v1/card/event` | 카드 이벤트 조회 |
| **카드 무이자 조회** | `GET` | `/v1/card/interest-free` | 무이자 할부 정보 |
| **승인금액 검증** | `POST` | `/v1/check-amount/{tid}` | Client 승인 모델 전용 |
| **망취소** | `POST` | `/v1/payments/netcancel` | 네트워크 장애 시 취소 ⭐ |

#### 도메인 정보

| 환경 | JS SDK | API |
|------|--------|-----|
| **샌드박스 (테스트)** | `https://pay.nicepay.co.kr/v1/js/` | `https://sandbox-api.nicepay.co.kr` |
| **운영계 (프로덕션)** | `https://pay.nicepay.co.kr/v1/js/` | `https://api.nicepay.co.kr` |

> ⚠️ **JS SDK 도메인은 동일**하나, **API 도메인은 환경별로 다름**

### 2.3 인증 방식

#### Basic Auth (권장)

```
Authorization: Basic <credentials>

credentials = Base64(clientKey + ":" + secretKey)
```

**예시:**
```javascript
const clientKey = 'S2_af4543a0be4d49a98122e01ec2059a56';
const secretKey = '9eb85607103646da9f9c02b128f2e5ee';

const credentials = Buffer.from(`${clientKey}:${secretKey}`).toString('base64');
// 결과: 'UzJfYWY0NTQzYTBiZTRkNDlhOTgxMjJlMDFlYzIwNTlhNTY6OWViODU2MDcxMDM2NDZkYTlmOWMwMmIxMjhmMmU1ZWU='

// API 호출 시
headers: {
  'Authorization': `Basic ${credentials}`,
  'Content-Type': 'application/json'
}
```

#### Bearer Token (선택)
- AccessToken을 먼저 발급받은 후 API 호출
- 토큰 만료 관리가 필요하므로 Basic Auth보다 복잡

### 2.4 결제창 호출 (JS SDK)

```html
<script src="https://pay.nicepay.co.kr/v1/js/"></script>
<script>
function serverAuth() {
  AUTHNICE.requestPay({
    clientId: 'S2_af4543a0be4d49a98122e01ec2059a56',  // 클라이언트 키
    method: 'card',                                      // 결제수단
    orderId: '유니크한-주문번호',                          // 주문번호 (unique)
    amount: 1004,                                        // 결제금액
    goodsName: '나이스페이-상품',                          // 상품명
    returnUrl: 'http://localhost:3000/serverAuth',        // 인증 결과 수신 URL
    fnError: function (result) {
      alert('개발자확인용 : ' + result.errorMsg + '');
    }
  });
}
</script>
```

#### JS SDK method 파라미터 (주요)

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| `clientId` | String | O | 클라이언트 키 |
| `method` | String | O | 결제수단 (`card`, `vbank`, `cellphone`, `kakaopay`, `payco`, `samsungpay`, `naverpay` 등) |
| `orderId` | String | O | 가맹점 주문번호 (Unique) |
| `amount` | Number | O | 결제금액 |
| `goodsName` | String | O | 상품명 (40byte 이내) |
| `returnUrl` | String | O | 인증 결과 수신 URL (Server 승인 모델) |
| `mallUserId` | String | | 가맹점 사용자 ID |
| `buyerName` | String | | 구매자 이름 |
| `buyerTel` | String | | 구매자 전화번호 |
| `buyerEmail` | String | | 구매자 이메일 |

### 2.5 승인 API 호출

결제창에서 카드사 인증이 완료되면, `returnUrl`로 인증 결과가 POST됩니다.

#### 인증 결과 응답 (returnUrl로 수신)

```json
{
  "authResultCode": "0000",           // "0000"이면 인증 성공
  "authResultMsg": "인증 성공",
  "tid": "UT0000113m01012111051714341073",  // ⭐ 거래 키 (승인 API에 전달)
  "clientId": "S2_af4543a0be4d49a98122e01ec2059a56",
  "orderId": "c74a5960-830b-4cd8-82a9-fa1ce739a18f",
  "amount": "1004",
  "authToken": "NICEUNTTB06...",
  "signature": "99ea68bf15..."        // ⭐ 위변조 검증값
}
```

#### 승인 API 호출

```bash
POST /v1/payments/{tid}
Host: api.nicepay.co.kr
Authorization: Basic <credentials>
Content-Type: application/json

{
  "amount": 1004
}
```

#### 승인 API 요청 명세

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| `amount` | Integer | O | 결제금액 |
| `ediDate` | String | | 전문생성일시 (ISO 8601) |
| `signData` | String | | 위변조 검증 Data (`hex(sha256(tid + amount + ediDate + SecretKey))`) |

#### 승인 API 응답 예시

```json
{
  "resultCode": "0000",
  "resultMsg": "정상 처리되었습니다.",
  "tid": "UT0000113m01012111051714341073",
  "orderId": "c74a5960-830b-4cd8-82a9-fa1ce739a18f",
  "status": "paid",
  "paidAt": "2021-11-05T17:14:35.000+0900",
  "amount": 1004,
  "balanceAmt": 1004,
  "goodsName": "나이스페이-상품",
  "payMethod": "card",
  "currency": "KRW",
  "approveNo": "000000",
  "receiptUrl": "https://npg.nicepay.co.kr/issue/...",
  "card": {
    "cardCode": "04",
    "cardName": "삼성",
    "cardNum": "123412******1234",
    "cardQuota": 0,
    "isInterestFree": false,
    "cardType": "credit",
    "canPartCancel": true,
    "acquCardCode": "04",
    "acquCardName": "삼성"
  }
}
```

### 2.6 취소·환불 API

```bash
POST /v1/payments/{tid}/cancel
Host: api.nicepay.co.kr
Authorization: Basic <credentials>
Content-Type: application/json

{
  "reason": "고객 요청",
  "orderId": "merchant-order-id",
  "cancelAmt": 1004          // 부분취소 시 사용, 미입력 시 전액취소
}
```

#### 결제수단별 취소/환불 가능 여부

| 결제수단 | 전체취소 | 부분취소 | 전체환불 | 부분환불 | 취소 가능 기간 |
|---------|---------|---------|---------|---------|--------------|
| 카드결제 | O | O | | | 1년 내 |
| 가상계좌 (발급 후) | O | | | | 입금 만료 전 |
| 가상계좌 (입금 후) | | | O | O | 180일 |
| 현금영수증 | O | O | | | 발급 당해 년도 |

### 2.7 망취소 (Net Cancel) ⭐ 매우 중요

> 결제 승인 API 호출 시 **네트워크 장애로 응답을 받지 못한 경우** 반드시 호출해야 합니다.

```bash
POST /v1/payments/netcancel
Host: api.nicepay.co.kr
Authorization: Basic <credentials>
Content-Type: application/json

{
  "orderId": "유니크한-주문번호"
}
```

- 망취소 유효기간: **1시간**
- 승인 요청 및 응답 수신에 실패한 경우에만 사용
- 일반적인 취소는 `/v1/payments/{tid}/cancel` 사용

### 2.8 현금영수증 API

```bash
POST /v1/receipt/
Host: api.nicepay.co.kr
Authorization: Basic <credentials>
Content-Type: application/json

{
  "orderId": "merchant-order-id",
  "amount": 1000,
  "goodsName": "나이스상품",
  "receiptType": "individual",     // individual(개인) / company(사업자)
  "receiptNo": "01012341234",      // 휴대폰번호 또는 사업자번호
  "supplyAmt": 250,               // 공급가액
  "goodsVat": 250,                // 부가가치세
  "taxFreeAmt": 250,              // 면세공급가액
  "serviceAmt": 250               // 봉사료
}
```

> ⚠️ 현금영수증 발급 요청 성공 → 영업일 D+1 국세청 전달 → D+2 발급 여부 확인 가능

### 2.9 웹훅 (Webhook)

나이스페이에서 결제 이벤트 발생 시 가맹점 서버로 알림을 보내는 시스템입니다.

#### 웹훅 발송 시점
- 💳 결제(승인) 되었을 때 (모든 지불수단)
- 가상계좌가 발급(채번) 되었을 때
- 가상계좌에 결제금액이 입금 되었을 때
- 결제가 취소 처리되었을 때

#### 웹훅 응답 규칙
```
HTTP Status: 200
Content-Type: text/html
Response Body: "OK"
```

> ⚠️ Response body에 **"OK" 문자열이 없으면 실패**로 처리됨

---

## 3. POC vs 실제 API 차이점 분석

### 3.1 구조적 차이

| 항목 | 현재 POC | 실제 나이스페이 |
|------|----------|---------------|
| **결제 시작** | 프론트에서 직접 카드번호 입력 | 나이스페이 JS SDK 결제창 호출 (`AUTHNICE.requestPay()`) |
| **카드 정보 처리** | 가맹점 서버에서 직접 처리 | 나이스페이 결제창에서 처리 (PCI DSS 준수) |
| **결제 승인** | 가맹점 서버에서 바로 Mock 응답 | 나이스페이 → returnUrl POST → 가맹점 서버 → 승인 API 호출 |
| **인증** | 없음 | Basic Auth (`clientId:secretKey` → Base64) |
| **데이터 저장** | In-memory Map | DB (PostgreSQL 등) |
| **위변조 검증** | 없음 | SHA-256 해시 검증 필수 |
| **에러 복구** | 없음 | 망취소 (netcancel) 필수 |
| **상태 알림** | 폴링 | 웹훅 (Webhook) |

### 3.2 가장 큰 차이점: 결제 흐름

#### 현재 POC (직접 처리)
```
사용자 → 카드번호 입력 → 가맹점 API → Mock 응답 → 결과 표시
```

#### 실제 나이스페이 (결제창 방식) ⭐
```
사용자 → 결제창 호출(JS SDK) → 나이스페이 결제창 → 카드사 인증 
→ returnUrl로 tid POST → 가맹점 서버에서 승인 API 호출 → 결과
```

> 💡 **핵심 포인트**: 실제 운영에서는 카드번호를 가맹점 서버가 직접 받지 않습니다!  
> 나이스페이 결제창이 카드 정보를 처리하고, 가맹점은 `tid`만 받아서 승인 API를 호출합니다.

---

## 4. 프로젝트 적용을 위한 필수 작업

### Phase 1: 🔑 계정 및 환경 설정 (1~2일)

- [ ] **나이스페이 회원가입**: [가맹점 관리자](https://merchant.nicepay.co.kr/) 사이트 회원가입
- [ ] **테스트 상점 생성**: 관리자 페이지에서 테스트 상점 생성
- [ ] **API KEY 발급**: 
  - `clientId` (클라이언트 키) - JS SDK 인증용
  - `secretKey` (시크릿 키) - API 인증용
  - 승인모델: **Server 승인** 선택
  - 인증방식: **Basic Auth** 선택
- [ ] **샌드박스 테스트**: 샌드박스 환경에서 테스트 결제 진행
- [ ] **방화벽 확인**: 나이스페이 API 도메인으로의 아웃바운드 통신 확인

### Phase 2: 🏗️ 핵심 구조 변경 (3~5일)

- [ ] **결제창 연동**: JS SDK `AUTHNICE.requestPay()` 구현
  - 프론트엔드에서 결제창 호출 로직 구현
  - `returnUrl` 엔드포인트 구현 (인증 결과 수신)
- [ ] **승인 API 연동**: `POST /v1/payments/{tid}` 호출 구현
  - Basic Auth 인증 헤더 생성
  - 위변조 검증 (`signData`) 구현
- [ ] **취소 API 연동**: `POST /v1/payments/{tid}/cancel` 구현
  - 전체/부분 취소 지원
- [ ] **망취소 구현**: `POST /v1/payments/netcancel`
  - 승인 API 호출 시 timeout 발생 시 자동 망취소

### Phase 3: 💾 데이터 및 보안 (3~5일)

- [ ] **데이터베이스 설계 및 연동**
  - 결제(transactions) 테이블
  - 취소(cancellations) 테이블
  - 현금영수증(cash_receipts) 테이블
- [ ] **위변조 검증 로직**: SHA-256 해시 기반 signature 검증
- [ ] **웹훅 엔드포인트**: `/api/webhook/nicepay` 구현
  - 결제 상태 변경 수신 및 처리
  - 응답: `200 OK`, Body: `"OK"`
- [ ] **환경변수 관리**:
  ```env
  NICE_CLIENT_ID=R1_xxxxxxxxx        # 운영: R1_ 또는 R2_ 접두사
  NICE_SECRET_KEY=xxxxxxxxx          # 시크릿키
  NICE_API_URL=https://api.nicepay.co.kr  # 운영: api, 테스트: sandbox-api
  ```

### Phase 4: ✨ 기능 확장 (5~7일)

- [ ] **현금영수증 API 연동**: `/v1/receipt/` 실제 구현
- [ ] **QR 결제 연동**: 카카오페이, 네이버페이 등 간편결제 연동
- [ ] **빌링(정기결제)**: 빌키 발급/승인/삭제
- [ ] **거래 조회 API 연동**: `/v1/payments/{tid}` GET
- [ ] **결제 영수증 PDF/프린트 출력**
- [ ] **관리자 대시보드**: 매출 통계, 정산 관리

### Phase 5: 🧪 테스트 및 배포 (3~5일)

- [ ] **단위 테스트**: API 클라이언트, 유틸리티 함수
- [ ] **통합 테스트**: 결제 흐름 E2E 테스트 (샌드박스)
- [ ] **운영 전환**: 
  - API 도메인 변경 (`sandbox-api` → `api`)
  - `clientId`, `secretKey` 운영키로 변경
- [ ] **에러 모니터링**: Sentry 등 설정
- [ ] **로깅**: 결제 관련 로그 체계 구축 (카드번호 등 민감정보 마스킹)

---

## 5. 당신이 봐야 할 자료 (숙지 사항)

### 📕 필독 자료 (반드시 읽어야 할 것)

#### 1️⃣ 나이스페이 공식 GitHub 매뉴얼 ⭐⭐⭐⭐⭐
- **URL**: [https://github.com/nicepayments/nicepay-manual](https://github.com/nicepayments/nicepay-manual)
- **읽는 순서**:
  1. `README.md` - Quick Guide (10분 내 TEST 개발 가능)
  2. `common/preparations.md` - 개발 준비 (회원가입, API KEY 확인, 방화벽)
  3. `common/api.md` - API·JS SDK (URI 목록, 인증 방식)
  4. `common/test.md` - 샌드박스 TEST 가이드
  5. `common/code.md` - 응답코드, 카드코드, 은행코드

#### 2️⃣ 결제 API 문서 ⭐⭐⭐⭐⭐
- `api/payment-window-server.md` - **결제창 Server 승인 모델** (가장 중요!)
- `api/cancel.md` - **취소·환불·망취소**
- `api/payment-receipt.md` - **현금영수증**
- `api/hook.md` - **웹훅**

#### 3️⃣ Node.js 샘플 코드 ⭐⭐⭐⭐
- **URL**: [https://github.com/nicepayments/nicepay-node](https://github.com/nicepayments/nicepay-node)
- Server Auth 모델의 Node.js 구현 예시
- Express.js 기반이지만 Next.js로 전환 가능

#### 4️⃣ 나이스페이 개발자 센터 ⭐⭐⭐⭐
- **URL**: [https://developers.nicepay.co.kr/](https://developers.nicepay.co.kr/)
- 최신 API 문서, SDK, 개발 도구

### 📗 참고 자료 (알면 좋은 것)

#### 5️⃣ 가맹점 관리자 페이지
- **URL**: [https://merchant.nicepay.co.kr/](https://merchant.nicepay.co.kr/)
- 상점 관리, KEY 발급, 웹훅 설정, 로그 확인

#### 6️⃣ APP 연동 가이드 (모바일)
- `api/app-ios.md` - iOS (Swift, Objective-C)
- `api/app-android.md` - Android (Java, Kotlin)
- 웹뷰 기반 결제창 연동 가이드

#### 7️⃣ 빌링(정기결제) 가이드
- `api/payment-subscribe.md` - 빌키 발급/승인/삭제
- 구독 서비스 등에 활용

### 📘 숙지해야 할 핵심 개념

| 개념 | 왜 알아야 하나 | 어디서 보나 |
|------|--------------|------------|
| **Server 승인 모델** | 결제 흐름의 기본, 실제로 이 모델을 사용 | `api/payment-window-server.md` |
| **clientId / secretKey** | 모든 API 호출의 인증에 필요 | `common/api.md` |
| **Basic Auth credentials** | API 호출 시 Authorization 헤더 생성 규칙 | `common/api.md#basic-auth` |
| **tid (거래 키)** | 결제 승인/취소/조회 모든 것의 기준이 되는 키 | `api/payment-window-server.md` |
| **signature / signData** | 위변조 방지를 위한 해시 검증 | `api/payment-window-server.md` |
| **returnUrl** | 인증 결과를 받을 가맹점 서버 URL | `api/payment-window-server.md` |
| **망취소 (Net Cancel)** | 네트워크 장애 시 결제 정합성 유지 **필수** | `api/cancel.md#망취소` |
| **웹훅 (Webhook)** | 결제 상태 변경 실시간 알림 수신 | `api/hook.md` |
| **PCI DSS** | 카드 정보 보안 규정 (가맹점이 직접 카드번호를 처리하면 안 됨) | 일반 상식 |

---

## 6. 실제 구현 로드맵

### 🗓️ 전체 일정 (예상 3~4주)

```
Week 1: 환경 설정 + 결제창 연동 (Phase 1 + Phase 2 일부)
  - Day 1-2: 나이스페이 회원가입, 키 발급, 문서 숙지
  - Day 3-5: 결제창 JS SDK 연동 + 승인 API 구현

Week 2: 핵심 API 연동 완료 (Phase 2 후반 + Phase 3)
  - Day 1-2: 취소/망취소 구현
  - Day 3-4: DB 설계 및 연동
  - Day 5: 웹훅 구현

Week 3: 기능 확장 (Phase 4)
  - Day 1-2: 현금영수증 실제 연동
  - Day 3-4: QR/간편결제 연동
  - Day 5: 관리 기능 (거래 조회, 통계)

Week 4: 테스트 및 배포 (Phase 5)
  - Day 1-2: 테스트 코드 작성
  - Day 3-4: 샌드박스 통합 테스트
  - Day 5: 운영 전환 및 배포
```

### ✅ 즉시 시작할 수 있는 첫 번째 액션

1. **나이스페이 GitHub 매뉴얼 읽기**: [https://github.com/nicepayments/nicepay-manual](https://github.com/nicepayments/nicepay-manual)
2. **가맹점 관리자 회원가입**: [https://merchant.nicepay.co.kr/](https://merchant.nicepay.co.kr/)
3. **테스트 상점 생성 및 키 발급**
4. **이 POC 프로젝트에서 `lib/nice-api.ts`를 실제 API 호출로 교체**

---

## 📌 최종 결론

### POC 평가
> ✅ **이 POC는 충분합니다.** 결제 흐름, 타입 정의, 컴포넌트 구조, API 설계 모두 프로덕션으로 확장 가능한 수준입니다.

### 핵심 변경 사항
> 🔄 **가장 큰 변경점**: 카드번호를 직접 받는 현재 방식에서 → **나이스페이 JS SDK 결제창 방식으로 전환**하는 것입니다.

### 추천 순서
> 1. 문서 숙지 (이 보고서 + GitHub 매뉴얼)
> 2. 나이스페이 계정 생성 및 키 발급
> 3. 샌드박스에서 결제창 연동 TEST
> 4. 기존 POC 코드를 단계적으로 실제 API로 교체
